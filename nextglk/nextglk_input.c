#include "nextglk.h"

char nextglk_wait_char(void)
{
    return '\0';
}

uint16_t nextglk_read_line(
    char* buffer,
    uint16_t buffer_size)
{
    (void)buffer;
    (void)buffer_size;

    return 0;
}